pygame>=2.5.0
# Use pyserial only — do NOT `pip install serial` (different, incompatible package).
pyserial>=3.5

# infer.py + train.ipynb (MobileNetV2)
torch>=2.0.0
torchvision>=0.15.0
numpy>=1.24.0
pillow>=10.0.0

# infer.py — Gemini naturalization
google-generativeai>=0.8.0

# train.ipynb only (evaluation / plots)
matplotlib>=3.7.0
seaborn>=0.13.0
scikit-learn>=1.3.0
